
--Para compilar el archivo de bison
bison -d parser.y


--Para compilar el archivo de flex
flex lexer.lex

--para flex y bison en conjunto
g++ parser.tab.c lex.yy.c -lfl -o salida

--para correrlo (por default jala el archivo in.parser)
./salida
